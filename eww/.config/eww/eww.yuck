(include "panel/top.yuck")
(include "dashboard/main.yuck")

(defwindow topbar
  :windowtype "desktop"
  :geometry (geometry :width "100%"
                      :anchor "top center")
  (top))

(defwindow dashboard
  :windowtype "dialog"
  :wm-ignore true
  :geometry (geometry :y "35px"
                      :width "50%"
                      :anchor "top center")
  (dashboard))
