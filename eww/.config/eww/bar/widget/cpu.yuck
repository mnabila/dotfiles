(defwidget cpu []
  (box :class "cpu widget"
       :orientation "h"
       :halign "center"
       :space-evenly false
    (label :class "icon" :text "")
    (label :class "value" :text "${round(EWW_CPU.avg, 0)}%")
  )
)
