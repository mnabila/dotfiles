(defwidget cpu [?icon]
  (box :class "widget cpu" :orientation "horizontal" :halign "center" :space-evenly true
    (label :class "icon" :text icon)
    (label :class "value" :text "${round(EWW_CPU.avg, 0)}")))
    

