(defwidget memory [?icon]
  (box :class "widget memory" :orientation "horizontal" :halign "center" :space-evenly true
    (label :class "icon" :text icon)
    (label :class "value" :text "${round(EWW_RAM.used_mem_perc, 0)}")))
    

