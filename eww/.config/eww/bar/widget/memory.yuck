(defwidget memory []
  (box :class "memory widget"
       :orientation "h"
       :halign "center"
       :space-evenly false
    (label :class "icon" :text "")
    (label :class "value" :text "${round(EWW_RAM.used_mem_perc, 0)}%")))
    

